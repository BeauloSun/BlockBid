import React from "react";
import { ListedHoldings } from "../components/ListedHoldings";
import { useParams } from "react-router-dom";

export const UserListedHoldings = () => {
  return (
    <div>
      <ListedHoldings />
    </div>
  );
};
